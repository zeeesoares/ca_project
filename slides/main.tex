\documentclass[handout]{beamer}

\usetheme{Frankfurt}
\setbeamertemplate{footline}[frame number]
\setbeamertemplate{navigation symbols}{}
\setbeamertemplate{caption}[numbered]
\setbeamertemplate{caption label separator}[period]

\usecolortheme{beaver}
\setbeamercolor{item}{fg=darkred}
\setbeamercolor{caption name}{fg=darkred}
\setbeamercolor{block title}{bg=darkred, fg=white}
\setbeamercolor{block body}{bg=gray!10, fg=black}

\urlstyle{same}
\def\arraystretch{1.5}
\def\singleitem#1{\begin{itemize}\item #1\end{itemize}}
\def\red#1{\textcolor{red}{#1}}

% PACKAGES -----------------------------------------------------------------------------------------

\usepackage{graphicx}
\usepackage{ragged2e}


% DOCUMENT INFORMATION -----------------------------------------------------------------------------

\title
    [Title]
    {Orchestration for efficient LLM Checkpointing within HPC}
\subtitle{\color{black} \small Advanced Computing Project 25/26}

\author[André Miranda, Gustavo Barros, José Soares, Miguel Carvalho]{
    \scriptsize
    \begin{tabular}{lc}
        André Miranda   & \color{gray} PG60238    \\
        Gustavo Barros  & \color{gray} PG61527    \\
        José Soares     & \color{gray} PG60277    \\
        Miguel Carvalho & \color{gray} PG61153
    \end{tabular}
}

\institute[DIUM]{Department of Informatics -- School of Engineering -- University of Minho}

\date[2026/04/24]{\scriptsize April 24\textsuperscript{st} 2026}



% Document Content ---------------------------------------------------------------------------------

\begin{document}

\frame{\titlepage}

\begin{frame}{Table of contents}
    \tableofcontents
\end{frame}

\section{Introduction}

\subsection{Context}
\begin{frame}{Context}
    \justifying
    Training Large Language Models (LLMs) involves refining billions of parameters over several weeks of continuous computation.
    
    \vspace{1em}
    To ensure resilience against hardware failures and prevent total progress loss, model states must be periodically saved to a Parallel File System (PFS).
    
    \vspace{1em}
    The challenge lies in optimizing this persistence process without compromising data integrity or resources utilization.
\end{frame}

\subsection{Problem}
\begin{frame}{Problem: The Checkpointing Overhead}
    \centering
    \small
    \begin{tabular}{l p{0.6\textwidth}}
        \hline
        \textbf{Challenge} & \textbf{Impact} \\ \hline
        \noalign{\smallskip}
        PFS Contention & Simultaneous writes from multiple nodes saturate the shared storage bandwidth. \\ 
        \noalign{\smallskip}
        Training Stalls & Heavy I/O operations block the training process, leading to significant idling. \\ 
        \noalign{\smallskip}
        Noisy Neighbor & Large-scale checkpointing bursts degrade performance for all concurrent HPC jobs.  \\
        \noalign{\smallskip}
        Storage Costs & Frequent writes escalate operational overhead and costs as model size grows. \\
        \hline
    \end{tabular}

    \vspace{1em}
    \footnotesize
    \textit{Table 1: Technical bottlenecks of direct checkpointing to PFS.}
\end{frame}

\subsection{Motivation}
\begin{frame}{Motivation}
    \small
    The goal is to design a checkpointing system that:

    \centering
    \begin{itemize}
        \small
        \item Mitigates I/O contention on the PFS, ensuring fair resource sharing and Cluster Harmony.
        \item Provides resilience against hardware failures, minimizing training disruptions.
        \item Optimizes checkpointing process and storage strategies to balance performance and costs.
    \end{itemize}
\end{frame}

\section{Approach and Architecture}
\subsection{Approach}
\begin{frame}{Approach: Policy-Driven Checkpointing}
    \centering
    \small

    \begin{columns}[t]
        \begin{column}{0.31\textwidth}
            \begin{block}{Tiered Persistence}
                \footnotesize \textbf{Local Storage} as a buffer, decoupling training from PFS latency.
            \end{block}
        \end{column}
        \begin{column}{0.31\textwidth}
            \begin{block}{Controlled PFS Access}
                \footnotesize Mitigate I/O contention via a centralized \textbf{Orchestrator}.
            \end{block}
        \end{column}
    \end{columns}

    \vspace{1em}

    \begin{columns}[t]
        \begin{column}{0.31\textwidth}
            \begin{block}{Policy-Driven}
                \footnotesize Dynamic migration based on cluster-wide scheduling policies.
            \end{block}
        \end{column}
        \begin{column}{0.31\textwidth}
            \begin{block}{Traffic Shaping}
                \footnotesize \textbf{Token Bucket} to smooth traffic and eliminate noisy neighbors.
            \end{block}
        \end{column}
    \end{columns}
\end{frame}

\subsection{Architecture}
\begin{frame}{Architecture}
    \begin{figure}
        \setlength{\abovecaptionskip}{0pt}
        \setlength{\belowcaptionskip}{0pt}

        \includegraphics[width=\textwidth]{assets/arch.png}
        \caption{
            System Architecture.
        }
    \end{figure}
\end{frame}

\section{Current Work}

\begin{frame}{Current Work}
    \begin{itemize}
        % \small

        \item \textbf{End-to-end checkpointing pipeline}:
        \begin{itemize}
            \item Checkpoints saved locally with asynchronous support for \texttt{torch.save}.
            \item Fallback to direct PFS copy if Migrater notification fails.
            \item Background migration to PFS.
        \end{itemize}

        \item \textbf{gRPC-based coordination layer}:
        \begin{itemize}
            \item PyTorch $\rightarrow$ Migrater: notify checkpoint completion + timestamp.
            \item Migrater $\rightarrow$ Orchestrator: checkpoint metadata + node state.
            \item Orchestrator $\rightarrow$ Migrater: \texttt{START\_FLUSH / CHANGE\_RATE / HOLD}.
        \end{itemize}
    \end{itemize}
\end{frame}

\begin{frame}{Current Work (Cont.)}
    \begin{itemize}
        % \small

        \item \textbf{Policy-driven scheduling}:
        \begin{itemize}
            \small
            \item \textbf{Fixed-Rate}: same bandwidth cap for every worker.
            \item \textbf{Uniform Fair-Share}: equal split across all registered workers.
            \item \textbf{Active Fair-Share}: equal split across workers with pending data only.
        \end{itemize}
    \end{itemize}
\end{frame}

\section{Evaluation \& Future Work}

\subsection{Next Steps}
\begin{frame}{Next Steps: Scaling and Benchmarking}
    \centering
    \small

    \begin{columns}[t]
        \begin{column}{0.35\textwidth}
            \begin{block}{Deucalion Deployment}
                \footnotesize Transition from local testing to the \textbf{Deucalion supercomputer} to validate scalability across multiple ARM/X86 nodes.
            \end{block}
        \end{column}
        \begin{column}{0.35\textwidth}
            \begin{block}{Policy Refinement}
                \footnotesize Evaluate different \textbf{scheduling policies} within the Orchestrator to find the optimal balance for PFS throughput.
            \end{block}
        \end{column}
    \end{columns}

    \vspace{1em}

    \begin{columns}[t]
        \begin{column}{0.35\textwidth}
            \begin{block}{Profiling}
                \footnotesize Extract performance data using the \textbf{PyTorch Profiler} during large-scale runs (already configured).
            \end{block}
        \end{column}
        \begin{column}{0.35\textwidth}
            \begin{block}{System Robustness}
                \footnotesize Ensure the Token Bucket performs reliably during massive concurrent transfers, preventing any bypass of the defined I/O policies.
            \end{block}
        \end{column}
    \end{columns}
\end{frame}

\frame{\titlepage}

\end{document}
